import '../Styles/Header.css'

export const Header = () =>{
    return (
        <header className="header">
            <h1>Pokemon Memory Game</h1>
            <h4>Get points by clicking on the image but don't click on any more than once!</h4>
        </header>
    );
}